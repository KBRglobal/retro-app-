{
  "name": "retro-app",
  "private": true,
  "version": "1.0.0",
  "description": "Retrospective Jira — reconstructs a real team's board, cost and human-time from git history.",
  "type": "module",
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "start": "node server.mjs"
  },
  "engines": { "node": ">=20" }
}
